sweep_name: smoke_v2_kalt
# Smoke-scale K-alternation: confirms the §v2.1 Part B schedule works under
# v2_probe alphabet (the K schedule itself is unchanged from v1 — this just
# guards against a v2 + K-alt combinator regression).
base:
  alphabet: v2_probe
  task: sum_gt_10_v2
  tape_length: 32
  n_examples: 16
  holdout_size: 0
  pop_size: 32
  generations: 10
  tournament_size: 3
  elite_count: 2
  mutation_rate: 0.03
  crossover_rate: 0.7
  backend: mlx
  log_every: 5
  arm: BP_TOPK
  topk: 3
  bond_protection_ratio: 0.5
  k_alternating_period: 5
  k_alternating_values: "3,999"
grid:
  seed: [0, 1]
