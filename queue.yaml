# Overnight queue — chem-tape v2 probe.
#
# Entries are executed in order unless they share a `parallel_group` number,
# in which case all entries in the same group run concurrently. Entries
# without parallel_group run sequentially as before.
# `expect_outputs` is relative to $RUN_DIR.
# Each sweep command invokes sweep.py with --output-root $RUN_DIR so all
# outputs land under the queue's per-entry directory. sweep.py writes
# sweep_index.json at the root of its output dir — that's our "done"
# sentinel.
#
# Compute budget per entry (per architecture-v2.md / prior v1 reference):
#   20 seeds × pop=1024 × gens=1500 × 4-worker Rayon ≈ 10-20 min per sweep.
# `timeout_seconds: 1800` (30 min) is ~2× the expected time — safe margin.
# The 3 seed-expansion entries inherit the same per-sweep cost.
#
# Ordering: pre-reg graded experiments first (§v2.1 → §v2.4), exploratory
# §v2.5 next, seed expansions + tape-length headroom last. §v2.4 is run
# regardless of pre-reg gating (gating is decided in morning triage).

- id: v2_1_partA_fixed_baseline
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_1_partA_fixed_baseline.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.1 Part A: sum_gt_10_v2 fixed baseline; produces F_10_v2 used by Part B and swamp-check."

- id: v2_1_partB_kalt
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_1_partB_kalt.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.1 Part B: K-alternation on sum_gt_10_v2."

- id: v2_2_pairA
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_2_pairA.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.2 Pair A: within-family slot indirection {R, E}."

- id: v2_2_pairB
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_2_pairB.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.2 Pair B: cross-family {R, upper}. v2 replication of v1.5a-binary."

- id: v2_2_fixed_baselines
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_2_fixed_baselines.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.1 swamp-check inputs: fixed baselines on string tasks R, E, upper_v2 (3 × 20 seeds)."

- id: v2_3_alternation
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_3.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.3 pre-reg: constant-slot indirection {sum_gt_5_slot, sum_gt_10_slot}."

- id: v2_3_fixed_baselines
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_3_fixed_baselines.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.3 fixed baselines on both threshold tasks (2 × 20 seeds)."

- id: v2_4_alternation
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_4.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.4 compositional depth {AND, OR}. Morning triage decides §v2.1 || §v2.3 gate."

- id: v2_4_fixed_baselines
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_4_fixed_baselines.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.4 fixed baselines for Fmin = min(F_AND, F_OR) threshold."

- id: v2_5_alternation
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_5.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.5 exploratory: aggregator-variation {sum, max}. Qualitative reporting only."

# ---- §v2.3 seed expansion (split 3-way per overnight-queue-runner design) ----

- id: v2_3_seeds_20_39
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_3_seeds_20_39.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.3 expansion block 1/3."

- id: v2_3_seeds_40_59
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_3_seeds_40_59.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.3 expansion block 2/3."

- id: v2_3_seeds_60_79
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_3_seeds_60_79.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.3 expansion block 3/3."

# ---- Tape-length headroom (low-priority housekeeping) ----

- id: v2_1_partA_tape48
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_1_partA_tape48.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 2400
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "Tape length=48 replication of §v2.1 Part A; settles the alphabet-density confound."

# ============================================================================
# Session 4 queue additions (2026-04-15) — decoder-variation batch
# Driven by Plans/prereg_v2_{11,12,13}*.md. Estimated total wall: ~80-120 min.
#
# §v2.8 (prereg_v2_8_integer_6token.md) and §v2.6'-Pair2 (prereg_v2_6_pair2_redesigned.md)
# Phase B sweeps are NOT queued — Phase A sampler audit FAILED on all candidate
# tasks for both. See Plans/prereg_v2_{8,6_pair2}_phase_a_HALT_record.md.
# Hard gate fired correctly; no compute wasted on swamped designs.
# ============================================================================

# §v2.11 — Arm A direct GP on §v2.3's load-bearing pair (highest priority)
- id: v2_11_arm_A_alt
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_11_arm_A_alt.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.11: Arm A alternation on sum_gt_{5,10}_slot. Tests load-bearing constant-slot-indirection mechanism survival under decoder variation."

- id: v2_11_arm_A_fixed
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_11_arm_A_fixed.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.11: Arm A fixed-task baselines on sum_gt_5_slot and sum_gt_10_slot. Required for Fmin_A and decoder-main-effect computation."

# §v2.12 — Arm A on proxy-basin tasks
- id: v2_12_arm_A_v2_4_natural
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_12_arm_A_v2_4_natural.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.12: Arm A on sum_gt_10_AND_max_gt_5 natural sampler. Tests whether max>5 proxy basin is BP_TOPK-specific."

- id: v2_12_arm_A_v2_4_decorr
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_12_arm_A_v2_4_decorr.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.12: Arm A on sum_gt_10_AND_max_gt_5_decorr (3-way stratified). Tests whether sum>10 next-best proxy basin is BP_TOPK-specific."

# §v2.13 — BP_TOPK(k=5) parameter sweep on §v2.3 and Pair 1
- id: v2_13_k5_v2_3_alt
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_13_k5_v2_3_alt.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.13: BP_TOPK(k=5) alternation on sum_gt_{5,10}_slot. Within-decoder-family parameter sensitivity."

- id: v2_13_k5_v2_3_fixed
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_13_k5_v2_3_fixed.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.13: BP_TOPK(k=5) fixed baselines on sum_gt_5_slot and sum_gt_10_slot."

- id: v2_13_k5_pair1_alt
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_13_k5_pair1_alt.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.13: BP_TOPK(k=5) alternation on Pair 1 (any_char_count_gt_{1,3}_slot)."

- id: v2_13_k5_pair1_fixed
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_13_k5_pair1_fixed.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.13: BP_TOPK(k=5) fixed baselines on Pair 1."

# §v2.14 follow-ups — all independent, run in parallel_group 14.
- id: v2_14d_consume_arm_a
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_14d_consume_arm_a.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  parallel_group: 14
  notes: "§v2.14d: consume rule under Arm A on Pair 1 (~15 min)."

- id: v2_14e_consume_E_preserve
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_14e_consume_E_preserve.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  parallel_group: 14
  notes: "§v2.14e: E-count pair preserve baseline (~15 min)."

- id: v2_14e_consume_E_consume
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_14e_consume_E_consume.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 1800
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  parallel_group: 14
  notes: "§v2.14e: E-count pair consume ablation (~15 min)."

# §v2.14c runs alone after the group — it's 4× compute and heavier.
- id: v2_14c_consume_4x
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_14c_consume_4x.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 7200
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.14c: consume + 4× compute on Pair 1 (~60 min)."

# ============================================================================
# Session 5 queue additions (2026-04-16) — Phase 1 batch
# Driven by:
#   Plans/prereg_v2-4-proxy-4-seeded.md    (§v2.4-proxy-4)
#   Plans/prereg_v2-14g-consume-arm-a-4x.md (§v2.14g)
#   Plans/prereg_v2-15-decoder-grid.md     (§v2.15)
# Target commit: 9455d04 (seeded-init infra + preregs + sweep YAMLs).
# Total estimated wall time: ~7-8h sequential.
#
# §v2.14f (non-MAP slot binding, alphabet extension needed) and §v2.16
# (regime-shift, DEAP infra needed) are DEFERRED — see their prereg files.
# ============================================================================

# Smallest first — fastest feedback + lowest risk.
- id: v2_4_proxy4_seeded
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_4_proxy4_seeded.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.4-proxy-4: seeded-init probe on §v2.4 AND (3 arms × 20 seeds = 60 runs, ~45 min). Splits 0/20 into discoverability vs maintainability."

- id: v2_14g_consume_arm_a_4x
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_14g_consume_arm_a_4x.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.14g: consume × Arm A × 4× compute on Pair 1 (~40 min). Tests whether Arm A null (§v2.14d) is rescued by compute."

- id: v2_14g_preserve_arm_a_4x
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_14g_preserve_arm_a_4x.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.14g companion: preserve-Arm-A-4× baseline (Option A, matched-commit contrast)."

- id: v2_15_grid_v2_3
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_15_grid_v2_3.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 14400
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.15 Part 1: decoder-ablation grid on §v2.3 pair (2×3 = 6 cells × 20 seeds = 120 runs, ~2-3h)."

- id: v2_15_grid_pair1
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_15_grid_pair1.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 14400
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.15 Part 2: decoder-ablation grid on §v2.6 Pair 1 (2×3 = 6 cells × 20 seeds = 120 runs, ~3h). Part 1 meta-learning gate."

# §v2.15-bp1-k3-nexp: n-expansion of the INTERMEDIATE cell (K=3, bp=1.0) on Pair 1.
# §v2.15 grid_pair1 landed this cell at 10/20 BOTH — INTERMEDIATE per prereg (9-11/20 band).
# Parent prereg commits to n-expansion for INTERMEDIATE cells. This sweep runs seeds 20-59
# (n=40) on the single cell; combined n=60 classification against ≥36/60 JOINT-LIFT threshold.
- id: v2_15_nexp_pair1_k3_bp1
  cmd: uv run python experiments/chem_tape/sweep.py experiments/chem_tape/sweeps/v2/v2_15_nexp_pair1_k3_bp1.yaml --output-root "$RUN_DIR" --workers 10
  timeout_seconds: 3600
  expect_outputs: [sweep_index.json]
  track: chem-tape-v2
  notes: "§v2.15-bp1-k3-nexp: n-expansion on INTERMEDIATE cell (K=3, bp=1.0) Pair 1 — seeds 20-59, ~8 min."
